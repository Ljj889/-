---
name: six-layer-orchestrator
description: 六层主 Agent 编排框架在 WorkBuddy 原生多 Agent 环境下的完整实现（v3.13.1，含调度器/架构师角色分离、项目级反馈回路、架构交叉验证、Red Flags 可检查信号、要不要编排出口、三种运行模式、粗拆细拆、引擎/智能分离与 LLM 局限防线、断点恢复 SOP、经验蒸馏与文档读取学习回路、AI 代码验收纪律、效果类主观验收、外部依赖探针、项目生命周期状态机、硬约束优先原则、设计锁定原则、每步方向讨论 Gate、混合审查 AI初审+用户终审、共享质量Rubric与收敛标准、并行隔离、Wave级复盘、计划评审回合制、僵尸能力红线（无调用点=僵尸）、过程文档先行+解耦第一+验收Gate不豁免、对抗式双架构师设计回路（rule 74）、调研配 crawler（rule 75）、合规检查脚本 six_layer_check（rule 64 引擎化）、引擎三件套脚本 checkpoint/cost_log/lifecycle 状态机硬拦截、蒸馏资产唯一真相源=独立仓库（rule 76））。当用户说"用六层框架做 X"、"六层编排"、"主Agent编排"、"按六层流程开发 X"、或要求先调研开源项目再拆解并行开发时使用。流程：Phase -2 调研（Swarm+轮询+经验召回，每步先与人讨论方向）→ Phase -1 规格 → Phase -1.5 人机设计锁定（设计文档+代码骨架，计划评审回合制收敛，用户逐项确认）→ Phase 0 初始化（版本控制前置）→ Phase 0.5 知识资产初始化（防文件腐烂+经验召回 Gate）→ L0-L5（调度器只路由不自执行；架构师/程序员/审查员/验收员为 Layer1 平级子 Agent；接口契约→编排→校验→混合审查→流程控制→工程保障）→ Phase 5 合并（用户终审）→ Phase 6 用户反馈回路 → Phase 7 经验蒸馏（写，落盘独立仓库）→ read_knowledge（读）闭环，越用越聪明。
agent_created: true
---

# 六层主 Agent 编排（WorkBuddy 原生版）

## Overview

把家俊的 CrewAI 六层框架在 WorkBuddy 原生多 Agent 能力上完整落地，并融合 2026 年多 Agent 生产级最佳实践：规格驱动开发（SDD）、Reflexion 反思回灌、编排模式决策、熔断与优雅降级、上下文管理。Orchestrator 由执行主 Agent 本人担任，子 Agent 用原生 Agent 工具启动。

## 与 Workspace 实例的关系（连接说明）

- **本技能是引擎**（通用、唯一被持续演化）。
- `多agent合作/`（公告板系统）= 同一思想的**第一次手写实例**，轮询方案不可行，已降级为方法论沉淀（只读）。
- `剪辑对话模式/对话剪辑/_orchestration/`（chatcut SDD 编排）= **第二次手写实例**，chatcut 项目专用；其实战 lessons（manifest 回传/即时审查/执行日志）已回灌进本技能（v3.1）。
- 场景路由与默认值见 workspace 根目录《多Agent协作_路由索引.md》。

## When to Use

- 用户要求按六层框架/主 Agent 编排方式开发项目
- 用户给蓝图（blueprint）、合约（contract）、技术选型、代码框架，要求拆模块并行开发
- 用户要求"拆 → 并行执行 → 独立审查 → 回退 → 合并"流水线，带成本记账与断点恢复
- 触发词：六层框架 / 六层编排 / 主Agent编排 / 按六层流程 / 拆模块并行开发

## Workflow（六层流程 v3.13.1 · 全局编号 1-76，删除号 13/15/17/25/26/30 留空不复用，新增从 46 起；有效规则 61 条 · 含 Phase 0.5 知识资产初始化防文件腐烂 · v3.9.1=ChatCut 反馈修订 / v3.9.2=调研经验转化 / v3.9.3=经验蒸馏+文档读取闭环 / v3.9.4=番茄钟实战反馈修订 / v3.9.5=多宠语音实战反馈 / v3.9.6=项目生命周期状态机 / v3.9.7=硬约束优先+纠错先检索 / v3.9.8=设计锁定原则 / v3.9.9=过程文档先行+解耦第一+验收Gate不豁免 / v3.9.10=对抗式双架构师设计回路+调研crawler / v3.10.0=每步方向讨论 Gate / v3.11.0=混合审查+质量Rubric+并行隔离+Wave级复盘+计划评审回合制 / v3.12.0=galagame 僵尸能力红线+monorepo构建顺序+web自动化环境坑+设置入口ADR / v3.13.0=WorkBuddy+CodeBuddy 两版合并统一编号（rule 73-75 并入，G-006 框架合规指南，引擎三件套 + six_layer_check 脚本）/ v3.13.1=蒸馏资产唯一真相源=独立仓库（rule 76，家俊决策 A））

### Phase -2：调研学习（可复用借鉴优先，家俊版核心前置）

> 目的：在拆解和写规格**之前**，先搞清"别人怎么做"。调研成果影响架构选型和任务拆解，所以必须前置。用户明确要求调研时必做；项目在成熟生态内（大概率有同类优秀项目）时默认推荐做。

1. **先召回先验经验，再定调研方向（v3.9.3 新增召回 · 学习回路读侧）**：主 Agent 读完输入材料后——① 先调 `read_knowledge(项目问题域/技术栈)`（rule 59）检索 `knowledge/_index.md`，已有 ADR/Rules 直接吸收为调研方向种子（避免重复调研已知坑），并在 `progress.json` 记录召回清单（L2 学习证据）；② 再产出**调研方向清单**（按项目特性定 2-5 个方向）：
    - 同类/竞品开源项目（别人怎么实现的）
    - 可复用库/依赖（直接拿来用的）
    - 可借鉴架构（分层、状态机、IPC、插件化等思想）
    - 可学习的设计模式与最佳实践（该领域特有的坑和套路）
2. 用 **Swarm 模式**并行派调研子 Agent：每个调研 Agent 负责一个方向，去网上搜索优秀开源项目/资料。任务说明含三件套 + 输出 schema，模板见 references/research_workflow.md。调研 Agent 默认上网工具配置见 rule 75（理解型用 web-scraper、硬站点/反爬用 hard-site-crawler-playbook、交互站用 agent-browser-core）。**派调研前先过 D1 方向讨论 Gate（rule 66）**：调研方向清单产出后，先与用户讨论确认方向，再派。
3. 每个调研 Agent 返回**调研报告**：找到的项目/资料链接 + **可复用点**（能直接用的）+ **可借鉴思想**（能学习的）+ **可学习框架**（能照做的）+ 每条的"为什么有价值 / 怎么落地到本项目"。
4. **调研结论分级（图片查看器实战教训）**：每条结论必须标注 `[事实]`（已实测验证）或 `[假设]`（来源可信但未在本项目实测）。`[假设]` 在后续执行中遇到不一致时**以实测为准**，更新 lessons.md 并回流修正调研决策——调研是输入不是圣经，版本号/兼容性这类硬指标以本机实测为准（案例：调研写"Electron 33→43/44"，实测 43.3.0 已是最新稳定）。
5. 主 Agent 汇总所有报告 → **逐条采纳决策**（采纳 / 不采纳 / 待定 + 理由）→ 产出 `research_adoptions.md`（采纳清单）。**回流前过 D2 采纳方向 Gate（rule 66）**：采纳/不采纳结论先与用户讨论确认，再进入成果回流。
6. **成果回流**：采纳内容融入项目理解——更新架构选型、完善 spec/plan/constitution（标注来源）、修正拆解思路。落盘 `research_report.md`（原始调研）+ `research_adoptions.md`（决策）。调研阶段产出的是**决策和输入**，不是代码，完成后进入 Phase -1。

### Phase -1：规格先行（SDD，推荐但不强制）

7. 若项目为生产级/多模块，先写规格四件套再动手（跳过则回到 vibe coding 风险）：
   - `spec.md`（需求+非功能约束+成功标准）、`plan.md`（架构/技术栈/组件）、`tasks.md`（可测任务）、`constitution.md`（不可协商原则：测试方法/约定/安全要求）。
   - 规格是 source of truth：改需求先改 spec 再重新生成，禁止边写边编。
8. 验收标准用 **EARS 语法**消除模糊："当 <触发条件> 时，系统应 <响应>"。模糊的验收标准是 Agent 自行发挥的根源。

55. **三种运行模式（v3.9 新增 · M_SKILL-2 #2）**：一套框架 + 三个参数组（**不分叉、不建三版技能**），Phase -1 调研完成 → 调度器按任务性质**推荐模式** → 用户确认 → 按对应参数组执行：

| 维度 | 高质量（基建/核心模块） | 性价比（日常开发） | 低成本（原型验证） |
|---|---|---|---|
| 架构师数量 | 2（交叉验证） | 1 + 架构评审 A2 | 1（无审查员） |
| 调研深度 | 三层全跑 | 两层（可行性+参照） | 一层（可行性） |
| 重试次数 | 3 | 2 | 1 |
| 人在回路 | 每模块确认 | 仅 FAIL 确认 | PASS 自动，FAIL 暂停 |
| 适用场景 | 基础设施、核心模块 | 日常开发 | 原型验证 |

> 调研深度行对应 references/research_workflow.md 的调研轮询法（高质量 5 方向×5 轮 / 性价比 3×3 / 低成本 2×2）。

### Phase 0：输入确认与工程初始化

9. 收集**项目材料包**（三类，缺一类就要标注假设或向用户补齐，模板见 references/spec_and_inputs.md）：
   - **① 蓝图/框架**：项目目标、功能范围、整体架构（骨架）→ 进 spec/plan
   - **② 技术选型**：技术栈、依赖、运行环境（工具）→ 进 plan
   - **③ 补充材料**：业务规则与限制、合规要求、代码风格/命名规范、验收标准偏好、环境约束（边界与血肉）→ 进 constitution / lessons / 验收标准。**这一类的缺失是 AI 用"合理默认"跑偏的头号来源，必须显式收集。**
10. **工程文件名唯一定义处**：`cost_log.md`（成本账本）、`progress.json`（断点）、`lessons.md`（教训文件）、`interfaces.md`（接口契约）这四个文件名**只在本条定义**，他处一律引用（canonical source 指针），禁止在别处另起文件名或硬编码路径。已存在则读取断点，跳过已完成模块。
11. **版本控制前置检查（P0 硬性，图片查看器实战教训）**：开工前检查项目是否在 git 版本控制下：
    - 无 git 仓库 → 先 `git init` + 提交当前基线（含所有现有代码），再开始任何改动；
    - 有 git 但基线未提交 → 先提交；
    - **高风险改动**（重构 / 换地基 / 依赖升级 / 数据迁移）**必须在干净的 git 基线上进行**——框架的 LOOP 审查回退是代码级，git 回退是基址级，两者都要有。无版本控制的代码上禁止高风险重构，直接拒绝开工并向用户说明。

### Phase 0.5：知识资产初始化（防文件腐烂，v3.7 新增）

> 目的：在项目初始化之后、角色定义之前，先把"知识资产不腐烂"立为硬步骤（源自《知识资产版本化规范》第五节）。让每个项目从第一天起自带可定位、可复用的经验/踩坑/思想沉淀，杜绝"文件越用越乱、不知放哪"。完整目录结构与模板以该规范为准。

11.1. **校准入口路由表**：开工前检查 `知识经验/_index.md` 是否存在；不存在则立即创建（唯一入口路由表，每条记录：`路径 | 类型 | 摘要 | triggers(触发关键词) | 更新日期`）。旧版本文件夹只读不删、绝不覆盖。
11.2. **判断版本边界**：对照 `全局设定.md` 触发条件——引入新设计思想/新框架、跨模块重构/架构调整、推进下一里程碑——任一项命中即开 `知识经验/v{N+1}/` 文件夹并写自描述 `README.md`（目标/范围/相对上版变更/文件清单）；均未命中则沿用当前版本。小迭代不新开文件夹。
11.3. **声明知识产出**：明确本次任务将产生的知识资产类型（`experience` 踩坑复盘 / `skill` 可复用流程 / `principle` 行为准则 / `insight` 规律认知），写入任务文件的"知识产出"字段，供结束前对账。
11.4. **强制落盘 + 索引更新**：任务结束前，按模板把踩坑/经验/思想落盘——文件名 `日期_主题.md`，frontmatter 含 `type / date / tags / confidence`（experience 加 symptom/root_cause/fix/recurrence_guard；insight 加 from_experiences）。落盘后立即在 `_index.md` 追加一行（<30 秒）。三类各一平层，禁止再嵌套子文件夹。

11.5. **经验召回 Gate（v3.9.3 新增 · 学习回路读侧）**：初始化时调 `read_knowledge(本次项目问题域/技术栈)`（rule 59）检索框架级 `knowledge/_index.md`，产出"本次项目相关经验召回清单"（资产路径 + 命中 tags + 注入位置：Rule→constitution / ADR→plan / Skill→按名调用），写入 `progress.json`。**Gate：未 read_knowledge 检索就标"已初始化 ✅" = 违规（Red Flag）**——不查索引不得声称"无相关先验经验"（NASA LLIS 覆辙：写了没人读）。召回的 Rule 红线注入 constitution 常驻，ADR 注入 plan 作 rationale，均标注来源路径。**实现者可见性强化（v3.9.5 新增 · 多宠语音实战 P10）**：召回清单必须**传递给实际实现者本人**（写进任务书/派发 prompt），不是调度器自己看过就算——实战教训：协议经验已入库但实现者未逐条对照，联调反复断连。

**调度器铁律扩展**：路由子 Agent 任务时，若子 Agent 产出可复用知识（踩坑/经验/思想）却未落盘 → 视为该子任务"**未验证完成**"，对接第 42 条未验证项清单，禁止标 `已验证 ✅`。

54. **索引 Gate 硬步骤（M_SKILL-9 #11 · rule 54）**：将"落盘后必须追 `笔记-知识资产索引.md`（或 `v1/_index.md`）一行"固化为硬 Gate——未追索引的子任务禁止标"已验证 ✅"，对接第 42 条未验证项清单与 Phase 0.5 调度器铁律扩展。Gate 动作 = `grep <new_file_path> 笔记-知识资产索引.md` 非空（索引落点沿用根级 `笔记-知识资产索引.md`，是否另建 `v1/_index.md` 属 D2 决策项，不在此固化）。

### L0 Agent 定义层（谁来做）

12. **角色分层（调度器不自执行，家俊关键修正）**：主 Agent 定位 = **调度器（Layer 0）**——职责只有：与人讨论产出蓝图、规划调研方向、路由任务、收集结果、判断错误类型、决策。**架构师 / 程序员×N / 审查员是 Layer 1 平级角色，全部由子 Agent 承担**（Layer 1 内部平级——架构师不指挥程序员，只有调度器能指挥任何 Agent）。**调度器绝不亲自拆模块、写代码、审查、写验证脚本。**
49. **调度器机械契约对齐豁免（M_SKILL-4 #4 · rule 49）**：下列情形调度器可直接做、不视为越权 Red Flag——① <10 行的机械改动（`mechanical_fix_lines < 10`）；② 模板适配（按 interfaces.md 字段对齐模板）；③ 文档落盘类环境修复（如目录名修复、知识落盘补 4 个 md）。超出此阈值的拆模块/写代码/审查/改数据仍须路由子 Agent。
14. 每个子 Agent 任务说明必须含**四要素：角色 / 目标 / 决策偏好 / 输出 schema**（schema 字段/类型/必填见 references/task_templates.md）。模板见同文件。
50. **契约验收前置核实（M_SKILL-5 #5 · rule 50）**：架构师在 interfaces.md 写某模块验收标准前，先核实代码/入口能否支撑（触发入口存在、测试钩子存在）；无法支撑则降级验收表述，禁止写"像素校验"式不可测验收。无支撑点的验收须标 `[不可测·已降级]`，而非沉默放行。
### L1 任务定义层（做什么 + 数据通道）

16. 共享任务列表记录每个模块（负责人、状态、验收标准），作为数据通道——子 Agent 之间不直接对话，数据由调度器中转。
18. **接口契约层（并行对接的唯一保障，chatcut 实战验证）**：拆解确认后、任何并行派发**之前**，必须先产出 `interfaces.md`（接口契约文档），统一所有模块的对接格式：
    - 每个模块：id、输入接口（`{interface, from, schema}`）、输出接口（`{interface, consumers, schema}`）、签名约定（函数名/参数/返回类型）、常量与事件名。
    - **所有子 Agent 的任务说明必须注入该契约全文**（只注入相关部分也行，但签名必须逐字一致）。
    - **审查员必须对照 interfaces.md 逐字比对**模块产出，签名漂移（改名/改参/改字段）直接 FAIL——这是并行开发防"接口对不上"的唯一手段。
    - 契约变更 = 先改 interfaces.md 再通知受影响的子 Agent，禁止各模块私下改接口。

### L2 编排调度层（按什么顺序）

19. **拆模块是架构师子 Agent 的活（家俊关键修正）**：调度器把蓝图 + 调研方向交给架构师，架构师产出模块清单 + interfaces.md 契约；调度器只做核对和分发，**不亲自拆**。**必查项（在 cost_log.md 留痕）**：必须存在一次架构师派发记录，且时间戳早于首个程序员派发记录；查无架构师派发却出现程序员派发 = 违反本条，FAIL。
    - **蓝图分层拆解（v3.9 新增 · M_SKILL-3 #3，LLM 注意力防线）**：大蓝图（多板块几百行）一次喂给架构师会超 LLM 信息处理上限，拆解质量下降。**改 Phase 1 为粗拆→细拆两趟（不建新框架，只改流程）**：
      - **Phase 1a 粗拆**：架构师读完整蓝图，**只输出板块级**（N 个板块 + 板块间接口定义），不要求模块级；**板块划分先过 D4 粗拆方向 Gate（rule 66）**——怎么切、按什么逻辑切，与用户讨论确认后再细拆；
      - **Phase 1b 板块审查**：架构评审 Agent 审查板块划分——粒度是否合理、板块间接口是否明确、有无遗漏；
      - **Phase 1c 逐板块细拆**：对每个板块，架构师读**该板块的蓝图片段（不是整个蓝图）**，输出板块内模块清单 + 接口定义；**处理完一个板块才能动下一个**；**每个板块的模块划分先过 D5 细拆方向 Gate（rule 66）**——模块怎么分、边界在哪、依赖方向，与用户讨论确认后再定契约；
      - **Phase 1d 模块审查**：架构评审 Agent 审查每板块的模块拆分。
    - **蓝图模板**：蓝图按板块组织（板块 1：xxx → 板块内模块 + 板块间接口；板块 2：xxx → ...；板块间接口总表）。
    - **prompt 首尾双写（v3.9 · LLM 首尾偏好防线）**：所有关键约束（契约条款、验收标准、禁区）在任务 prompt **开头 + 结尾各写一遍**——LLM 对 prompt 中间的信息吸收最弱。
    - **架构师产出自检项（v3.9.1 · ChatCut F-04 实战教训）**：架构师产出模块清单 + interfaces.md 契约后，落盘前先做一次**交叉引用自检**——① 依赖 DAG 表中的每个依赖，必须在 interfaces.md 的 consumers 声称中有对应（谁依赖谁 ↔ 谁消费谁，三处文档必须一致）；② 每个模块的验收测试文件归属明确（架构计划的测试布局 ≠ 任务验收测试文件 = 矛盾）；③ 字段语义无歧义（同名不同义必须显式区分，如"白名单 id vs 云端 id"）。自检发现不一致先自行修订再交付评审，减少评审 REVISE 轮次。
20. **架构交叉验证（家俊提议）**：架构师是"主观输出"角色（设计无客观测试标准），单一视角有盲区——**输出越主观的角色越需要交叉验证**（架构方案 > 审查结论 > 代码，代码跑测试即可）。按项目复杂度分两档：
    - **轻量档（默认，所有项目）**：架构师拆解后，派**架构评审 Agent**（独立第二视角）复核方案——模块边界是否清晰、接口契约是否完备、依赖是否合理、有无更优拆法；输出 PASS / 具体修改意见，回灌架构师修订后定稿。
    - **重量档（复杂/高风险项目：多模块强耦合 / 换地基 / 长期维护）**：**2-3 个架构师并行独立拆解**，调度器对比方案差异 → 选最优或融合 → 用户拍板。
    - **成本控制**：架构师是 reasoning 档最贵角色，默认用轻量档，不默认 2-3 个。
47. **档位-复杂度匹配表（M_SKILL-2 #2 · rule 47）**：档位枚举沿用第 38 条 `lite / default / reasoning`。匹配规则——① <50 行机械改动 → 默认 `lite`；② 大脚本（≥100 行）派发前必须附骨架/伪代码，否则调度器应降级或要求补骨架后再派；③ 仅复杂设计才用 `reasoning`。**默认档位过高是超时主因（06 复盘 P0-1），必须按复杂度压档。**
21. **依赖强度 + 风险决定模式（图片查看器实战验证）**：拆解时**标注依赖（DAG）**：无依赖并行派发，有依赖按拓扑序；模式选择不只按"任务类型"，还要看依赖与风险：编排模式选型（Parallel Fan-out / Sequential / Router / Supervisor-Worker / Hierarchical / Swarm）见 references/orchestration_patterns.md 决策矩阵，不在此重复。
    - 模块**无依赖** → 并行（Fan-out），如 Wave 1；
    - 模块**强依赖链 + 高风险**（重构/换地基/升级）→ **Sequential Gate（串行门控）**：每阶段完成 + 独立验证通过（Gate）后才进下一阶段；阶段内部仍可小并行（如两个都依赖 store.js 但彼此无关的模块可 fan-out）。并行前提不成立时不要硬并行。
22. **同验证面合批**（打包配置类实战教训）：**同验证面 = 两个任务的验收动作逐字相同**（同一条命令／同一份产物／同一个人工检查步骤）。判定法：分别写出两个任务的验收命令，字符串不等就不许合批。合批省成本、独立 Gate 保定位：验收方式相同的任务合批为一次实测，但开发态各自独立 Gate 以定位故障。**显式补充（v3.9.1 · ChatCut F-06 实战教训）**：**验收命令指向同一文件（尤其同一测试文件）的模块必须合批，禁止并行写同一文件**——并行写同一测试文件会互相覆盖（D2/D3 共用 test_fancytext_draft.py 案例）。判定："验收命令含相同文件路径"= 必须合批，即使断言内容不同。
23. **阶段提交（Gate = 里程碑提交点）**：每个 Gate / Wave 完成并验证通过后，执行 `git commit` 打一个里程碑提交。**回退点粒度 = 阶段级**：出错时要么代码级 LOOP 修补，要么 git checkout 回到上一阶段基线，两条路都要通。
24. 大项目拆解方案先展示给用户确认再分发（= **D6 派发前总确认**，rule 66）；**派发原子核对（chatcut 实战教训）**：并行派发前先列出"本轮应派模块清单"（N 个模块 id），派发后**逐个确认已发出**——声称并行却只发出部分模块（其余事后补）是"假并行"，必须当场发现并补发或报告，禁止"以为派了其实没派"。
69. **并行隔离（v3.11.0 新增 · 工程控制优先，不靠自觉）**：并行子 Agent 之间靠"只写自己的文件"是**自觉纪律**（rule 64：能硬约束的绝不靠自觉）——有 git 的项目，并行模块用**独立 git worktree / feature 分支物理隔离**（业界：独立 worktree 是"零协调开销"的工程控制，直接消除互相覆盖的一类故障）：
    - **隔离粒度**：除"同验证面合批"（rule 22）外的并行模块，每个子 Agent 一个独立 worktree/branch；**只合回经过审查的产物**。
    - **合并 = 集成验证点**：合回主分支时跑全量测试；冲突模块标"待仲裁"——**仲裁权在调度器+用户，不让 Agent 私下解决**（列出冲突双方改动，用户拍板）。
    - **无条件退路**：无 git 环境时降级为"模块文件所有权纪律"，但必须在 cost_log 记一行 `未隔离风险声明`（rule 64 引擎化信号：能隔离却未隔离 = 待加固）。
    - **与断点恢复衔接**：worktree 名 = `wt-模块id`，写进 progress.json 预期产物清单，重接时 `git worktree list` 一次定位。

### L3 数据规范层（输出格式对不对）

27. 返回后用 `scripts/validate_output.py` 校验，格式不合格**把具体报错回灌给子 Agent 重做**（等价 Pydantic 自动重试 + 错误上下文），禁止手动容忍畸形输出。
28. **代码回传纪律**（chatcut 实战验证）：JSON 里永远不放全量代码——大代码（≥300 行）子 Agent 写文件落盘 + 回传紧凑 manifest（文件清单/自检/集成备注），审查员直接读文件审。防止爆 token 与截断损坏。
29. **契约核对硬步骤（chatcut S6 实战教训，防"该建的没建"）**：每个模块完成后，**自动比对 manifest 文件清单 vs 实际磁盘**——清单里声明该有的文件（含 `__init__.py`、资源文件、配置文件）逐个核对存在性；缺失当场 FAIL 并要求补建，禁止只信 manifest 自报。**文件所有权从"君子协定"升级为"硬校验"。**
52. **审查员磁盘 stat 核对（M_SKILL-7 #7 · rule 52）**：审查员除 manifest 清单比对外，增加磁盘 `stat` 存在性硬步骤——`stat.exists(path) && size>0`（路径存在且非空）。自报"已落盘"须以磁盘 stat 独立证实，否则视为未验证（治"自报≠实盘"，06 复盘 P1-2）。

### L4 流程控制层（出错怎么办 / 依赖怎么排）

31. **审查时机（即时逐模块）**：每模块完成后**立即**审查并决定合入/重做，禁止"全部跑完再统一审查"——早审查早暴露，失败成本低。
32. **LOOP 反馈闭环 + Reflexion 反思**（核心增强）：
    - 审查 FAIL → 审查员产出**具体修改要求**（不是"再改改"）→ 回灌给子 Agent → 重审，同一模块最多 3 轮。
    - **重试不等于改进**：任何重试都必须带失败上下文（报错/审查意见），禁止无反馈盲重试。**回退重派时必须注入 `previous_code` + `review_feedback`**（原第 17 条，禁止盲改）。
    - 失败教训写入 `lessons.md`（"尝试 X → 失败(原因) → 换 Y → 成功"），防止同类错误重复发生。
48. **审查 Gate 前置（M_SKILL-3 #3 · rule 48）**：派发审查 Agent **之前**，先用 `grep <signature> <disk_path>` 确认被审查模块的关键签名已落盘（对照 interfaces.md）；未确认落盘则禁止派审查，先回退架构师收尾。对应 06 复盘 P0-2 防"读中间态误报"。Gate 动作 = grep 关键签名返回非空。**复核补强（v3.9.4 新增 · 番茄钟 B2 实战）**：**审查报告里的"缺失/FAIL"项，调度器先磁盘复核（stat + grep）再决定回退**——架构师修订与审查并行时，审查可能读到修改中的文件，把"已修复的"误报为"缺失"（番茄钟 06 复盘 P0-2：6 处 FAIL 全是误报）。复核通过 → 不回退，标注"审查中间态误报"。
33. **失败分类 + 路由（家俊关键修正，调度器最核心的能力）**：审查报告 FAIL 时，调度器**判断错误类型 → 路由到正确角色，自己不修**：
    - **代码级错误**（签名错、漏接口、变量名不一致）→ 路由给对应程序员 N（只重跑这个模块）；
    - **架构级错误**（模块拆分不合理、接口定义冲突、依赖环）→ 路由给架构师（重新调研、重新拆）；
    - **合约级问题**（合约条款过严、格式要求冲突）→ 路由给**调度器**（调度器+人修订合约）→ 架构师重新拆。**合约是调度器+人定的，架构师没权限改合约**——路由给架构师改合约是错误路由（IMA 参考手册 T1 修正）；
    - 或 git checkout 回退到上一阶段基线（基址级兜底）。
34. **熔断 + 优雅降级**：同一子 Agent 连续失败 3 次 → 停止重派该 Agent，换思路或向用户升级；单个模块失败不应拖垮整个项目——其余模块先交付部分结果并说明。
35. **人在回路（两种模式，执行前确认用户选哪种）**：
    - 标准模式：每模块 PASS 后展示给用户确认（接受 / 修改 / 跳过）再合并；遇到置信度低或需求模糊时主动升级给用户。
    - **全权执行模式**（用户不在场时）：用户预授权后，拆解方案按合理默认直接执行，模块审查通过不打断用户，只在最终交付时输出验收报告；中途遇到架构级分歧、连续熔断或需求模糊时才升级给用户。
36. **数据迁移 / 语义变更 → 必须问用户（图片查看器实战教训）**：涉及改变数据含义或存储方式的任务（如收藏从"物理复制"改为"引用"、字段重定义、索引重建），执行前**必须**向用户确认迁移策略（保留 / 自动迁移 / 手动认领 / 清理孤儿数据），禁止默认处理。历史数据中没有映射信息的"孤儿数据"（如旧收藏无 source 映射）必须让用户拍板，禁止 AI 自行决定去留。**全权执行模式下此条不豁免**——数据不可逆，永远升级给用户。
37. **子 Agent 输出异常协议（chatcut 实战教训）**：子 Agent（含审查 Agent）返回**空结果 / 非法 JSON / 明显截断 / 网络错误（502/超时等）**时——**先磁盘核验，再决定处置**（v3.9.1 修订，ChatCut 花字开发断点重接实战教训）：
    - **第一步：核验预期产物清单**。对照派发时写在 `progress.json` 的"预期产物清单"（模块 id × 目标文件 × 状态 pending）——`git status` + `stat 目标文件` + `grep 关键签名`（对照 interfaces.md）。
    - **第二步：产物在 → 收下**。"空结果/502 = 结果回传失败 ≠ 任务没执行"——磁盘产物在，则视为"已执行未回报"，核验签名后收下，**不重派**（重派会浪费一轮档位成本）。
    - **第三步：产物不在 → 重派**。按原协议：自动重派一次（带异常说明）；重派仍异常 → 换一个不同的子 Agent 重做（路由换人），并向用户说明。
    - **禁止凭"空结果"直觉直接重派**——先查磁盘，磁盘才是唯一真相（rule 39 三态同理）。
    - **错误信息完整透传（v3.9.4 新增 · 番茄钟通话联调实战）**：**失败原因必须可见才能定位**——子 Agent/工具返回错误时，把具体错误信息（Error 帧 message、堆栈、响应体、状态码）**完整透传给上层**，禁止只显示"连接失败"这类笼统描述（豆包 Realtime 联调 startWaiter 机制：等服务器确认，Error 帧 reject 带 message——错误可见了 5 轮根因定位才成立）。笼统错误 = 让下游猜 = 二次排查成本。
    - **调度器不亲自兜底做知识工作**——v3.4 写"主 Agent 亲自复核"是错的，违反调度器铁律。核验产物是磁盘检查（git status/stat/grep），不是知识工作，调度器可做。
67. **混合审查：AI 初审 + 用户代码终审（v3.11.0 新增 · 联合写代码核心）**：AI 审查（rule 31-33/48）只是**第一遍筛查，不是终审**——AI 审 AI 有盲区（业界：AI 审查建议采纳率仅 16.6%，>50% 未采纳的 AI 建议是错误或可替代方案；保留人工验证流程的团队生产力收益持续 >12 个月概率是移除者的 2.8 倍，Stanford 2026.03）——**最终裁决权必须留给人**：
    - **两级分工**：初审（AI 审查员按 rule 68 Rubric 逐项筛查，标常见问题）→ 终审（调度器整理"终审卡"给用户，用户三态裁决：接受→合并 / 修改→带意见回灌程序员 / 打回→按 rule 33 失败分类路由）。
    - **终审卡格式**（一屏以内，调度器转述、不甩原始报告）：`模块 | 改动范围(diff 摘要) | AI 审查结论(Rubric 打分) | 待决问题 | 请确认：接受 / 修改 / 打回`。
    - **展示范围与打扰控制**：低风险小改动（<50 行、无接口变更、非数据/安全相关）**批量合并终审**（一次看多个）；关键模块（接口变更/数据迁移/安全相关/用户点名模块）**逐模块终审**；全权执行模式（rule 35）不逐模块打断，但 Phase 5 合并前必须展示全量 diff 终审，**终审通过才允许进 DELIVERED**（衔接 rule 63 生命周期）。
    - **代码评审与验收测试分离（v3.11.0）**：终审前两道 AI 动作职责分开——**评审员**（读代码：契约/接口/质量/安全，rule 68 维度）与**验收员**（跑测试：功能正确性/边界/真实路径）。防止"跑测试的人"因测试通过跳过深度审阅；低成本模式（rule 55）可合并为一人，**关键路径必分离**。
68. **共享质量 Rubric + 收敛标准（v3.11.0 新增 · 防多 Agent 无限循环）**：审查 PASS 不是泛泛通过，必须按**项目级 Rubric**逐项打分（防 Coder-Reviewer 因无共享质量标准而无限循环）：
    - **Rubric 七维度**（constitution 定义，可增删）：① 功能正确性（测试真跑/边界/真实路径）② 接口一致性（interfaces.md 逐字比对）③ 复杂度（圈复杂度目测，超标标 P2）④ 重复代码（与现有代码 grep 查重，应抽公共逻辑标 P1）⑤ 死代码/幻数（未引用代码、硬编码常量）⑥ 安全（密钥泄露/注入/路径穿越——AI 生成代码安全问题最高 2.74 倍）⑦ 可维护性（命名/docstring/结构/错误处理）。
    - **打分制**：每维度 `P0(必改) / P1(应改) / P2(建议)`；**PASS 条件 = 无 P0 + P1 有明确修改计划**；任一 P0 → FAIL 打回。
    - **收敛标准**：同一模块审查最多 3 轮；每轮须带具体修改要求且**Rubric 有 P0→无 的实质改善**，否则判定循环空转（rule 57 重复熔断）→ 换思路或升级用户。
    - **验收测试与代码评审分离的落点**：评审员出 Rubric + 结论，验收员出测试证据（--junitxml 数字），两者都进终审卡。

### L5 工程保障层（花了多少 / 崩了怎么办 / 上下文怎么管）

> 唯一需主 Agent 自律的层：WorkBuddy 无内置记账/断点，cost_log.md 与 progress.json 必须主动维护，忘写就丢；L3 强度靠"schema + validate_output.py 校验 + 打回重做"逼近框架强制，校验必须跑脚本不靠肉眼。

38. **执行日志（CostTracker 升级版）**：每次派发在 `cost_log.md` 追加**一行定长记录（可 grep）**：`[时间] | [派发对象] | [档位 lite/default/reasoning] | [预估成本] | [结果 成功/失败/时长]`。合并时输出汇总。**git log 是最完整的执行审计轨迹**——Gate 提交点即审计点，交付报告可引用提交号佐证。
53. **cost_log 异常类型列（M_SKILL-8 #8 · rule 53）**：第 38 条定长记录 `时间|派发对象|档位|预估成本|结果` 增加第 6 列 `异常类型`，升级为 `[时间]|[派发对象]|[档位]|[预估成本]|[结果]|[异常类型: 超时/空/非法JSON/截断/无]`。一次派发结果为失败/异常时，第 6 列须记录具体异常类型，缺列或空值视为记录不合规（并在第 42 条未验证项清单注明"异常类型须进 cost_log"）。
57. **成本硬刹车（v3.9.2 新增 · 调研经验 E1 转化）**：**监控告警 ≠ 调用前拦截**——成本控制必须在调用发生**之前**执行（三档递进）：
    - **硬迭代上限**：每个循环任务（LOOP/Wave/反思）设 `maxIterations`（默认 3，复杂任务可调）；超限即停并报告，禁止 `while True` 无界循环。
    - **tokenBudget + 熔断**：每任务/每模块设 token 预算（默认 20000）；**调用前**检查累计消耗，超预算即熔断停止（circuit breaker），不是跑完才看账单。
    - **重复熔断（repetitionThreshold）**：连续 N 轮输出与上轮高度重复（如同段文本反复出现）→ 判定循环空转，停止并升级给人——概率系统不能当确定性流水线，状态只增不减就是失控信号。
    - 平台引擎层（编排 B）将实现为**调用前 enforcement**（拦截函数），非仅 cost_log 事后记账。
70. **Wave 级复盘（质量-优化回路，v3.11.0 新增）**：经验蒸馏（Phase 7）是项目级回路，但**波次内也要微调**——每 Wave/批交付后，调度器做 1 分钟复盘并落盘，把"上一波踩的坑"直接变成"下一波的派发参数"：
    - **复盘卡**（cost_log.md 一行 + progress.json 记录）：`[Wave N 复盘] | 成功模式 | 失败模式 | 档位匹配(过高/合适/过低) | 任务说明质量(骨架给了吗/收窄了吗) | 下波调整`；
    - **调整落到下一 Wave 派发**：档位压降/升档、任务说明补骨架、收窄探索范围、合批策略（rule 22）、隔离粒度（rule 69）；
    - **复盘失效红线**：连续 2 个 Wave 出现**同一失败模式**而未调整派发参数 → 未验证项清单标注"复盘失效"，禁止继续加派（rule 57 重复熔断的 Wave 粒度版）。
39. **Checkpoint（子 Agent 回报 + 三态，chatcut 实战教训）**：状态不靠调度器单方面记录——**子 Agent 完成任务时必须回报**（已产出哪些文件、测试是否通过）；调度器核对后写 `progress.json`，状态分三态：`已交付`（产物在磁盘）/ `已验证`（测试绿）/ `未验证`（没跑过测试或测试红）。中断恢复时按三态判断，**不靠翻文件猜**。
    - **派发前写"预期产物清单"（v3.9.1 新增，ChatCut 断点重接实战教训）**：并行派发前，在 `progress.json` 记录——`Wave N 派发: [模块id]→[目标文件列表]，状态 pending`。重接/异常核验时，`git status` + `stat` 对照清单**一次定位**哪些模块"已执行未回报"、哪些"未执行"，不用逐个文件猜。预期产物清单是 rule 37 磁盘核验与断点恢复 SOP 的前提。
40. **上下文管理**（防"长对话记忆爆炸"）：单任务上下文目标 **< 2,000 行；> 5,000 行必须裁剪**（拆子任务或写 scratchpad）。主上下文只放当前必要信息；任务细节、模块清单、文件 map 写进 scratchpad，按需读取；子 Agent 任务说明必须自包含；教训文件 lessons.md 长期保留，每次重开先读（环境坑如 safe-delete 吞输出、tmp 目录删不掉、模型下载被拦——全部记入 lessons.md，重开先读）。
    - **压缩前落盘钩子（Pre-Compaction Flush，v3.9.2 新增 · 调研经验 M1 转化）**：任何上下文压缩/状态清理**之前**，必须先把将被丢弃的内容落盘（scratchpad/progress.json/checkpoint）——**压缩即失忆**是长任务重启后丢早期消息的头号原因。顺序固定：先落盘 → 再压缩 → 压缩后校验落盘成功才继续。平台引擎层（编排 B）将实现为压缩函数的强制前置钩子。
41. **自测与验证归因（chatcut 实战教训）**：派发前自检任务说明与 schema；确定性的事用代码不用 LLM。**验证失败时必须先以最小样例复现，并写明归因（脚本错／代码错）**；未写归因就回退代码 = 违规。验证脚本本身也会出 bug——先自检"是验证脚本错还是被测代码错"再判，禁止把脚本 bug 误判为代码失败。
42. **验证证据纪律 + 未验证项清单**：任何"已验证 ✅"的能力声明都必须有运行日志佐证；无证据的声明一律视为未验证。**交付报告必须显式列出"未验证项"清单及原因**（环境限制 / 成本 / 时间）——如"转录链路因沙箱拦截模型下载未经真实验证"，禁止让用户以为"全绿 = 全通"。
43. **验证两层制（chatcut dry-run 实战教训，最危险的一层）**：明确区分两种验证——
    - **流程验证（dry-run）**：断言"步骤存在、命令非空、逻辑分支走通"——便宜、快、可天天跑；
    - **效果验证（真实执行）**：断言"输出真的产生了预期结果"（如导出尺寸真的是 1920×1080、成片真的有字幕）——**关键路径至少一次真实冒烟，不许只靠 dry-run**。
    - dry-run 全绿 ≠ 真能用：合成测试数据太"干净"，盖不住真实世界噪声（浮点边界、缺失依赖、环境差异）。测试要求含**边界值 + 容差 + 脏数据**（如 2.39999s 不能因无容差被 [0,2.4] 判越界）。
    - **"不跑不会发现"原则（v3.9 新增 · M_SKILL-4 #5，LLM 能力遗漏防线）**：AI 生成代码的**正常路径看起来完整，但边缘条件（静默降级、分类逻辑、兜底覆盖、跨语言格式冲突）只有真实执行才能暴露**——测试用例必须覆盖这些边缘条件，审查段不只读代码，还要跑代码（关键路径真实冒烟）。已知案例（4 个）：① ima 静默回退（连败 3 次后偷偷降级）；② phase2_run.py 缺失 DeepSeek 分支（兜底完整但主路径没写）；③ M3 分类优先级错误（noncode > integration）；④ contract @interface 跨语言格式冲突。
    - **幻觉防线（v3.9 · LLM 幻觉编造防线）**：① 模块输出用显式 `<TEST>...</TEST>` / `<REPORT>...</REPORT>` 标签分离，**审查只验代码段**（REPORT 段叙述瑕疵不判 FAIL 但标注）；② **REPORT 段不得提不存在的路径**（编造路径=幻觉信号）；③ **审查范围 = 模块全部输出**（CODE + TEST + REPORT），不只代码段——防止编造路径的叙述瑕疵溜进主分支。
    - **交互路径验收（v3.9.4 新增 · 番茄钟 C4 实战）**：**验收必须覆盖"真实交互路径"（事件链/用户旅程），不只功能钩子**——dev 钩子直接调主进程、绕过鼠标事件链的验收，测不出"点击不切情绪"这类真实用户才会触发的 bug（番茄钟桌宠实战：自动化验收过但真实鼠标微抖触发拖拽死区）。验收设计时自问："这条路径是用户真实操作能走到的吗？"
    - **验收优先序反转 + False Green 认知（v3.9.4 新增 · 业界 AI 代码测试调研）**：AI 生成代码的失败画像 ≠ 人类代码——测试优先级反过来：**① 完整用户旅程 E2E（真实浏览器/真实数据）> ② 错误/空状态路径（AI 很少写 unhappy-path，提示词没点名就不写）> ③ 边界假设核对（AI 猜的字段/权限/返回类型/状态码，对照真实生产者）> ④ 现有流程回归（改了 X，所有 import X 的旧流程都要跑）> ⑤ 单测垫底**。核心认知：**绿 CI = AI 代码最会伪造的信号（False Green）**——单测断言的是"结构"不是"行为"，AI 改接口/改语义后旧测试照过但行为已错（promo-code-as-string 案例：集成边界类型错，下游静默强转，无测试断言最终金额）。验收用**意图型测试**（按用户意图断言："点提交 → 验确认消息"，不绑 DOM 选择器/内部实现——重构不坏）；等状态转移不等 sleep；跨边界断言业务结果不只 DOM。

### Phase 5：合并交付

44. 全部 PASS + 用户确认后合并产物，写最终交付（含运行说明），输出成本汇总与完成报告。交付前对照 spec.md 逐字核对（若走了 SDD），报告里明确标注每条能力"已实测 / 仅设计 / 未验证（原因）"，并按第 42 条格式列出**未验证项清单**。**发布即下一轮循环的起点——进入 Phase 6，不结束。**
51. **收尾双核对（M_SKILL-6 #6 · rule 51）**：Phase 5 合并前对每模块做四重核对——`contract✓ / dispatched✓ / on_disk✓ / verified✓`（契约有 + 已派发 + 已落盘 + 已验证）。任一缺失禁止标 PASS，须回退补齐（重做第 24 条派发原子核对于收尾，治 M09 漏派）。

56. **引擎/智能分离 + 引擎层硬拦截（v3.9 新增 · M_SKILL-5 #6）**：
    - **两层唯一接口（架构分层）**：引擎层 → 智能层 `{ "prompt": "...", "role": "architect", "temperature": 0.0 }`；智能层 → 引擎层 `{ "content": "...", "usage": { "prompt_tokens": N, "completion_tokens": M } }`。**引擎层禁止包含任何 LLM 调用逻辑以外的"智能判断"**——判断归调度器/子 Agent，执行归引擎。当前 WorkBuddy 版引擎层暂由提示词驱动，迁移时替换为 Python 硬代码。
    - **硬拦截三件套（LLM 静默降级 / 能力遗漏 / 温度敏感防线）**：① **解析失败绝不静默回退**——JSON schema 解析失败/连败即停，写 `PHASE_X_FAILED` 报告，禁止偷偷换低质量路径（如静默退回兜底模式）；② **import 后立即检查核心函数是否存在**——引擎层加载模块后马上核对关键导出，防"兜底完整但主路径没写"（能力遗漏）；③ **不同阶段用不同温度**——框架段/拆解段 `temperature=0.0`（稳定），反思段/反思回灌 `temperature=0.3`（产真洞察），禁止全程一刀切。
    - **心跳报告刷新上下文（LLM 上下文污染防线）**：长轮询/长任务子 Agent **每轮重读约束文件 + 输出心跳**（"Agent N 等待中，已等待 X 分钟"）刷新上下文，防止跑 30 轮后忘记自己的角色和约束。

### 断点恢复 SOP（v3.9.1 新增 · ChatCut 花字开发 2 次断点重接实战沉淀）

> 触发：会话中断后重接 / 子 Agent 返回异常（空结果/502）后需要确认现场。**对接成本全部来自"没有标准断点记录，重接靠猜"**——本 SOP 与 rule 37（先磁盘核验）、rule 39（预期产物清单）配套使用。

```
【重接第一步】git status → 列出未提交/未跟踪文件，识别异常残留（如 .pytest_basetemp/）
【重接第二步】对照 progress.json 的"预期产物清单"逐模块 stat：
  清单里的文件全在 → 该模块"已执行未回报"，进入第三步核验
  清单里的文件缺失 → 该模块"未执行"，补派
【重接第三步】grep 关键公开函数签名（对照 interfaces.md）→ 确认不是半成品
【重接第四步】跑该模块测试（--junitxml 拿数字）→ 绿则收下，红则按 rule 33 失败分类路由
【重接第五步】清理异常残留（.pytest_basetemp/ 等）→ 更新 progress.json 三态 → 继续下一波
```

**核心原则**：空结果/502/超时 = **结果回传失败 ≠ 任务没执行**。先查磁盘（git status/stat/grep），磁盘才是唯一真相；产物在就收下核验，不在才重派。禁止凭"空结果"直觉直接重派（会重复执行已完成工作）。

### Phase 6：项目级反馈回路（家俊关键修正，区别于代码级 LOOP）

45. **循环工程在项目级的闭环**：交付 → 用户使用 → 反馈 → **调度器接收并归类**（新需求 / 缺陷 / 架构不满）→ **路由给架构师**（理解新需求 → 更新蓝图 → 重新拆）→ 新一轮执行。
    - **代码级 LOOP** = "FAIL → 带反馈重写"（L4）；
    - **项目级 LOOP** = "用户用了不满意 → 架构师重新理解需求 → 重拆 → 重执行"。
    - 调度器只接收反馈和路由，**不亲自改**；用户反馈是触发新一轮 Phase -2 调研 + Phase -1 规格更新的输入。
    - 这条把框架从"一次性流水线"变成"持续迭代的循环工程"。

46. **「要不要编排」出口（v3.7 新增）**：派活前先问——这个任务**单 Agent 直做行不行**？若行（任务小到没有"全局"可看、调度器 domain value 归零），就直做，并在 `cost_log.md` 记一行理由（如"小改动，无全局视野需求"），**不进入六层编排**。只有当任务需要多角色 / 多模块 / 交叉验证时才编排。这是防"为编排而编排"的出口（精准补丁反模式 A：纯路由仅在该任务无 domain value 时才退化成反模式）。
    - **Bug-Fix 轻量模式（v3.9.2 新增 · 家俊需求）**：**报错修复**是此出口最高频场景，触发时走轻量流程——① 先看日志/复现找根因（强制，找不到不许动手）；② 按根因范围选路：单文件单根因→直做 / 跨模块根因不明→低成本模式编排（rule 55）/ 涉及数据语义变更→问人（rule 36）；③ 改完必验证（rule 43）+ 交付说明根因。完整流程见 references/bug_fix_mode.md。

### Phase 7：经验蒸馏（v3.9.3 新增 · 写侧，越用越聪明）

> 目的：每个阶段/项目结束，把踩坑/决策/可复用能力沉淀为**框架级资产**（`knowledge/` 四桶），供下一项目 `read_knowledge` 召回——防止 NASA LLIS 式"写了没人看"。Phase 6 反馈回路是 Phase 7 的输入；Phase 0.5 声明产出、rule 54 索引 Gate 是落盘骨架，**不重造，只衔接**。完整设计见 references/knowledge_loop.md。

58. **两级触发 + 六步流程（v3.9.3 新增）**：
    - **阶段级 mini**（每 Phase/Gate/波次后）：更新 `lessons.md`；在 cost_log/progress.json 记一行 `候选: <资产路径>|<类型>|<一句话>`——便宜、必跑；
    - **项目级 full**（Phase 5 合并交付后、进入下一轮前，必跑）：六步 **Gather（收集 lessons/cost_log/反馈台账/审查 FAIL）→ Rewrite（统一句式「当 A → 易犯 B → 护栏 C」，含可执行护栏）→ Classify（四桶：rules/decisions/framework-skills/guides）→ Gate（人工把关，用户逐条确认采纳/修改/驳回）→ Land（落盘 + rule 54 索引 Gate 追 `_index.md`）→ Feed（更新索引，标已蒸馏防重复）**。蒸馏由 sub-Agent 产出候选，模板见 references/task_templates.md「蒸馏 sub-Agent」。
59. **read_knowledge 文档读取原语（v3.9.3 新增 · 读侧，闭环另一半）**：签名 `read_knowledge(query, scope="project", status_filter="active", top_k=5) -> list[Asset]`——① 读 `knowledge/_index.md`（canonical entry，**禁止盲搜**）→ ② 按**问题类型/tags** 匹配（不按项目名——NASA 覆辙）→ ③ 过滤 scope（user/project/local）+ status（默认 active）→ ④ 返回资产（frontmatter+摘要）供注入。触发点：Phase -2 调研前（方向种子）/ Phase -1 规格（Rule 红线进 constitution、ADR 进 plan）/ Phase 0.5（召回清单 Gate）/ 执行中 just-in-time 召回（子 Agent 遇未知情形按需调）/ Phase 7 写前（查重防重复造）。**护栏：先读索引；只读 active；过期（last_verified 超 review_cycle）标 stale 依赖前复核；引用标注来源路径不静默吸收；精度优先（precision > recall）防上下文膨胀（呼应 rule 40 <2,000 行）；召回资产是建议/约束不替代本次判断。**
60. **学习回路闭环 + L2 证据（v3.9.3 新增）**：Phase 7 写 → `knowledge/` 资产库存 + `_index.md` → 下一项目 read_knowledge 读 → 注入使用 → **`progress.json` 记"本次召回清单"（资产路径+命中 tags+注入位置）= L2 学习证据（教训真改变了流程）+ 防腐烂审计依据**。防腐烂治理：frontmatter 必带 `version/owner/last_verified/review_cycle/status/tags/source`；`deprecated` 不自动注入；Superseded 旧版只读不覆盖；**已嵌入框架规则（SKILL.md rule）的资产可从库移除（空库=吸收彻底）**；每个新项目启动 = 天然复核点。
76. **蒸馏资产唯一真相源 = 独立仓库（v3.13.1 新增 · 家俊 2026-09-02 决策 A）**：Phase 7 产出的框架级资产（`knowledge/` 四桶新文件 + `_index.md` 更新 + SKILL.md 规则修订）**必须直接落盘到独立仓库** `D:\Ai\一些ai项目\项目2.0\six-layer-orchestrator\`——**平台 skills 目录只是运行时副本，不是真相源**（WorkBuddy/CodeBuddy/Codex 三处 = 分发目标，随仓库同步覆盖）。落盘四步清单：
    - **① Land**：资产写入仓库 `knowledge/`（或修订仓库 `SKILL.md`），追 `_index.md`（rule 54 Gate）；
    - **② Commit**：`git add -A` + `git commit -m "knowledge: <一句话摘要>"`（PowerShell 下执行，凭据 GCM 已配）；
    - **③ Push**：`git push origin master`（远程 `github.com/Ljj889`；PowerShell 下 push 稳定，Git Bash 偶发 SIGTERM 假象）；
    - **④ Sync**：`bash sync_to_platforms.sh` 分发三处 + `--check` 校验（仓库 → WorkBuddy/CodeBuddy/Codex）。
    - **违反红线**：只更新平台 skills 目录、不落仓库 = 资产视为未验证（rule 54 逻辑延伸）；**禁止在平台目录直接改 skill**——下次 sync 会被仓库覆盖。遇到"当前会话加载的是平台副本"时，改仓库路径后同步即可。
61. **效果类需求主观验收（v3.9.5 新增 · 多宠语音对话实战 D1/D4/D5）**：任务含**主观体验**（语音情绪"像不像"、画面"顺不顺眼"、交互"自不自然"）时——**dry-run/单测绿 ≠ 效果对**（AI 伴侣语音实战：111 测试全绿但情绪"听不出"，到 W4 人耳才暴露）。要求：
    - **Wave 前置主观验收 Gate**：效果类任务在 Wave 划分时就设"人耳/MOS 验收点"（W1 就设，禁止拖到最后 Wave 才人耳验收）；
    - **分层验证定位根因**：效果类调试用**维度交叉**（如音色×资源×提示词×强度 4 维），一次只动一维，锁定根因；
    - **维度化评分（业界 MOS/CMOS）**：不只听"好不好"——拆维度（自然度/情感符合度/韵律/音质）各打 1-5 分（MOS，ITU-T P.800）；迭代对比用相对分（CMOS："这版比上版好多少"），**盲测**（隐藏版本信息防先入为主）；个人项目可用"自己盲测 + 维度化"简化版，不必 20 人；
    - **试错预算上限（D5）**：效果攻坚设"试错轮数/金额上限"，超限强制暂停做决策，禁止无限合成测试；
    - 审查清单加"主观效果预检"（D4）：效果类模块审查时要求附音频/样本并记录主观判断（即便审查 Agent 抓不了效果质量，也要留预检记录）。
62. **外部依赖探针（v3.9.5 新增 · 多宠语音对话实战 D2/D3/D6）**：外部 API/第三方依赖**"文档说的" ≠ 真实行为**（AI 伴侣实战：任务书写"context_texts 支持复刻音色"实为仅官方音色，到 W4 才发现方向错）。要求：
    - **W0/Phase 0 外部依赖探针任务**：对外部 API 的每个关键能力各写 1 个**最小调用验证**（探针脚本）——文档/任务书的"支持"字样一律不采信，实测为准；
    - **契约"待实测"标注（D3）**：外部接口在 interfaces.md 里标"待实测"而非"已锁定"，实测后允许快速修订（不走慢变更流程）；
    - **调研 [假设] 保留不确定性（D6）**：调研结论标 `[假设]` 的，任务书引用时保留"待实测"标注（框架 rule 5 的 [事实]/[假设] 分级延伸到任务书）；
    - **第三方依赖版本锁定**：关键依赖锁版本（实战案例：websockets 13 `extra_headers` vs 旧版 `additional_headers`，版本差异直接断连）；协议/格式类问题先找权威文档（bug_fix_mode v1.1 已强调）。
    - **主力路径最先验证（经验 6）**：多路径架构（主/降级）时，**先打主力路径**（Wave 1-3 默认走 HTTP 降级、主力 Realtime 到 W4 才联调 = 反例），防止主力路径验证被无限推迟。
63. **项目生命周期状态机（v3.9.6 新增 · 多宠语音项目实战反馈）**：框架是"提示词纪律"不是"硬状态机"——主 Agent 会忘自己是调度器（上下文漂移）+ Bug-Fix 出口易被滥用 → **"验收没过就自己改"的根因**。补上显式生命周期状态：
    - **五态**：`INIT → EXECUTING → DELIVERED（已交付·等待验收）→ REOPENED（反馈中）/ ACCEPTED（已验收）`；
    - **核心约束：DELIVERED/REOPENED 状态下，一切修改请求 = Phase 6 反馈 = 调度器必须路由给子 Agent**（架构师改契约 / 程序员改代码 / 审查员验证），**禁止调度器直改——不论改动多小**（rule 49 机械豁免不适用于"验收未过"的项目）；
    - **验收判定权归用户**：只有用户明确说"完成/没问题/可以了/验收通过"才进 ACCEPTED（框架模式退出）；之后的修改/新需求 = 新迭代（重新 Phase -2，不默认沿用原调度上下文）；
    - **状态落地**：项目生命周期状态写入 `progress.json` 的 `lifecycle` 字段；调度器在 Phase 5 交付时标 DELIVERED，用户确认后标 ACCEPTED，反馈修改时标 REOPENED；
    - **Bug-Fix 模式限定**：rule 46/55 的"单文件单根因直做"出口**仅适用于已 ACCEPTED 项目的独立 bug**；未验收（DELIVERED/REOPENED）项目的修改一律走分发——验收没过 = 项目没完成 = 框架模式持续生效。
64. **硬约束优先原则（v3.9.7 新增 · 家俊实战核心洞察）**：**LLM/Agent 对话多轮后自然遗忘规则（物理规律，不是态度问题）**——提示词纪律只作兜底，**凡是能引擎化/脚本化的约束必须由代码强制**。判定与落地：
    - **发现"靠自觉"的约束 = 应引擎化的信号**：任何规则若靠调度器"记得"执行，就是隐患——自动列入引擎化候选（清单核验、磁盘 stat/grep、状态机、沙箱执行、lifecycle 拦截、cost_log 记账等皆可脚本化）；
    - **引擎化优先级**：① 数据类校验（清单/签名/三态）→ 脚本/CLI 子命令 ② 状态流转（lifecycle/checkpoint）→ 状态机硬拦截 ③ 执行验证（沙箱真跑）→ 已引擎化 ④ 纯判断类（架构评审/审查）→ 无法引擎化，保留提示词+多 Agent 制衡；
    - **分层哲学**：能硬约束的绝不靠自觉（引擎），不能硬约束的靠结构制衡（角色分离/审查交叉），最后才靠提示词纪律（兜底）；
    - **纠错先检索（家俊实战洞察 2）**：改 bug/排错时，**禁止直接"猜/撞"**——必须按序：① `read_knowledge` 查自己的经验库（rule 59：有没有遇到过一样的问题）→ ② 网上搜索同类问题解法（WebSearch，复用不重造：别人踩过的坑别自己再撞）→ ③ 最小复现归因（rule 41）→ ④ 才动手改。检索过的证据要标注来源（复用 rule 59 引用溯源）。
65. **设计锁定原则（v3.9.8 新增 · 家俊设计洞察：设计不能全交给 Agent）**：**项目的框架设计图 + 每个部分/功能的代码骨架必须先做出来，且不能完全交给 Agent 自由发挥（至少现在做不到）——人机共同讨论、逐项确认每个细节，再进入开发**。这不是架构师（框架师）的任务——架构师只负责拆解分发/完善/上网调研设计图。落地：
    - **Phase -1.5 人机设计锁定层**（在 Phase -1 规格之后、Phase 0 之前）：AI 基于调研+规格给出**逐项设计建议**，边讨论边**上网调研佐证**（rule 64 检索纪律），**用户逐项确认**（Gate）；产出三类：① `design.md`（完整详细：每模块职责/接口/数据结构/关键算法/外部依赖实测结论/边界）② `skeleton/`（每个功能的可编译骨架：函数签名/类结构/接口占位/伪代码）③ `decisions_pending.md`（已确认/待定决策清单，讨论留痕）；
    - **执行自由发挥最小化**：`design.md` + `skeleton/` 是执行的**上限约束**——程序员只"填肉"不改设计；发现设计缺陷必须回 Phase -1.5 重新确认（禁止执行中私自改设计）；
    - **引擎支持**：`cli.py design <dir>`（生成讨论提纲+模板）/ `cli.py design <dir> --finalize`（锁定 Gate：校验 design.md+skeleton 存在 → lifecycle 记 design_locked → 之后执行禁止改设计）；
    - **与既有规则衔接**：设计中的外部接口沿用 rule 62"待实测"标注；决策点用 rule 5 [事实]/[假设] 分级；数据类决策沿用 rule 36 问人。
71. **计划评审回合制（R1/R2→CLEAN 收敛，v3.11.0 新增）**：Phase -1.5 设计锁定的"一次确认"升级为**回合制收敛**——把歧义消解前置到成本最低阶段（业界协议：计划 → 计划评审 R1/R2 → CLEAN → 实现）：
    - **回合状态机**：设计文档/骨架产出后标 `R1` → 架构评审（rule 20 轻量档）+ 用户审查 → 修订后 `R2` → ... → **CLEAN**（无 P0 异议 + 用户点头）。状态写入 `progress.json` 的 `design_round` 字段。
    - **收敛上限**：同一设计最多 3 回合（R3）；第 3 回合仍未 CLEAN → 停下问用户——设计方向问题回 rule 66 D-Gate 重走，禁止硬闯（防"评审-修订"空转，rule 57 重复熔断）。
    - **Gate（P0 硬性）**：`design_round == CLEAN` 之前**禁止进入 Phase 0 派发**——"设计没收敛 = 实现必然返工"。
    - **与 rule 65/66 分工**：rule 65 定义"设计锁定该产出什么"（内容），rule 66 定义"方向先对齐"（每步方向 Gate），本条定义"怎么判定设计已收敛"（流程）——三者叠加为：方向对齐 → 内容锁定 → 回合收敛，再进实现。

72. **无调用点的能力 = 僵尸（v3.12.0 新增 · galagame 关系系统双重僵尸实战）**：**能力定义 ≠ 能力落地**——模块"接口完整、类型检查全绿"但**没有任何宿主调用它**（galagame `scanRelationshipState` 全仓零调用点、备份双实现互为第二真相源、idb 未托管单例从未 init 的写入被静默吞掉），这类"僵尸能力"比死代码更隐蔽：类型系统不报错，验收也测不出。护栏：① **新能力必须有显式宿主接线点**（启动即扫/定时器/UI 入口/消费方），写进模块验收标准（衔接 rule 50 契约前置核实）；② 验收前 `grep 能力名` 查调用方，**无调用点 = 僵尸，禁止标「已验证」**（rule 39 三态），按未验证项处理并回退补接线；③ 修复/审查发现疑似缺陷时先 grep 调用方定位真实链路（rule 64 纠错先检索），无调用方的"缺陷"是死代码而非缺陷（galagame 备份误判 P1 复盘）。

66. **每步方向讨论 Gate（v3.10.0 新增 · 家俊需求：调研/分析/拆解过程每一步先和人讨论"设计方向"，而不是讨论设计细节）**：
    - **为什么**：从拿到目标 → 调研 → 规格 → 拆解 → 框架文档 → 派发，每一步都是方向性决策（往哪走）。**方向错了，细节越精确越浪费**——细节可以在设计锁定（rule 65）里逐项确认，但**方向必须边做边和人对齐**，不能等整个框架文档成型才给人看。这是"与人相互讨论完善"的机制，不是单向汇报。
    - **方向 vs 细节**：方向 = 做什么、为什么、往哪走、备选方向、风险（如"板块按业务域切还是按技术层切"）；细节 = 具体参数、字段、函数签名、实现方式（留给 design.md/skeleton 与编码）。**方向讨论永远不沉入细节**——讨论中冒出的细节问题记入 decisions_pending.md"待定"清单，到设计锁定阶段处理。
    - **方向 Gate 清单（每个方向性步骤完成后、进入下一步前，调度器先向用户提交方向建议卡）**：
      - **D1 调研方向**（Phase -2 派调研前）：调研哪几个方向？为什么是这几个？
      - **D2 采纳方向**（调研报告汇总后）：哪些采纳/不采纳？选型方向是否因此调整？
      - **D3 规格方向**（Phase -1 规格初稿后）：要解决什么问题、成功标准、技术路线是否认可？
      - **D4 粗拆方向**（Phase 1a 板块拆解后）：板块怎么切？按什么逻辑切？
      - **D5 细拆方向**（Phase 1c 每板块细拆后）：模块怎么分、边界在哪、依赖方向是否认可？
      - **D6 派发方向**（interfaces.md 契约后、首次派发前）：整体方向 OK？可以开工？
    - **方向建议卡格式**（一屏以内；调度器转述架构师/调研产出，**不把原始报告直接甩给用户**——与人讨论是调度器职责，rule 12）：
      ```
      当前步骤 | 我的建议 | 为什么 | 备选方向 | 风险 | 请确认：继续 / 改方向 / 我有想法
      ```
    - **用户回应三态**：① 继续 → decisions_pending.md 记"已确认"，进入下一步；② 改方向 → 按新方向重做当前步骤再继续；③ 我有想法 → 并入方向后回到对应 Gate 重走。**每次讨论都留痕 decisions_pending.md**（与 rule 65 同文件，避免另起文件名）。
    - **与 rule 65 分工**：rule 65 设计锁定 = 方向定了之后的**细节逐项确认**；本 Gate = 每个方向节点先确认**方向**。叠加为"**方向先对齐、细节后锁定**"，防止"方向跑偏到一半才发现"和"过早纠缠细节"两个极端。
    - **裁剪边界（rule 55 低成本模式）**：用户明确"批量确认"时可把 D1-D6 合并为 2-3 次提交（如 D1+D2、D4+D5），**但方向讨论本身不可省略**；全权执行模式（rule 35）须用户**明确预授权**"方向也不用问我"才可跳过，否则仍生效——方向不可逆地影响后续所有细节与成本。Bug-Fix 轻量模式（rule 46）与"要不要编排"出口（单 Agent 直做，rule 46）不受影响。

73. **过程文档先行 + 解耦第一 + 执行模式验收 Gate 不豁免（v3.9.9 新增 · 番茄钟语音实战 · v3.13.0 两版合并纳入）**：小迭代/B-tuning 也是"开发"，必须遵守——① **过程文档先行**：每轮先写开发过程文档（诊断→决策→改动→复验→结论，含真实行号+片段），再出成果；成果之前必有过程记录，方便调错/对接/优化（落地：当轮开发对接文档对应节 + 跨会话进 `.workbuddy/memory/YYYY-MM-DD.md` + 长期 MEMORY.md）；② **解耦第一**：新增/修改功能优先独立成纯函数模块/组件、走接口/类型契约、可独立替换测试，不往现有文件堆、不跨层直接依赖具体实现（范例：`classifyReplyEmotion` 抽成 `reply-emotion.ts` 纯函数 + provider 第三参透传，零 IPC/渲染端改动）；③ **执行模式下的验收 Gate 不豁免**：即便用户授权"继续开发/用六层框架去调研研讨有开发的就继续开发"，**代码变更仍须经用户拍板验收**（rule 63 DELIVERED/REOPENED 约束持续生效）——"授权开发"= 授权 AI 跑 调研→研讨→落码 循环，≠ 跳过验收；落码后出交付报告 + 验收 Gate 清单，用户复验通过才 ACCEPTED；④ **证据交付强化（rule 42）**：改动须有真实行号+片段，论断有代码/日志实证，typecheck + 导入真实模块的单测（非复刻逻辑）才算验证，禁止"已修复"式无证据表述、禁止"全绿=全通"。

74. **对抗式双架构师设计回路（v3.9.10 新增 · 家俊 2026-08-19 立 · v3.13.0 两版合并纳入）**：将 rule 20 重量档升级为"非平凡设计/讨论阶段默认"——架构师 A/B 互不可见独立提案 → 交叉互审 → 挑刺 Agent（Red Team）批 → ≤3 轮熔断升级人；配套 references/adversarial_design.md 含 proposal/review/critique/consensus 四套模板与回合协议。**门控护栏**：只用于非平凡设计（rule 46/74），小设计仍轻量档单架构师——避免 Anthropic/Manus 警告的"组织架构图式"过度分工。**外部验证**：multi-agent debate（Du et al. 2023）实证多实例提案+互批提升推理/事实准确率；Anthropic/Google ADK 子 Agent 独立上下文隔离原则支撑"互不可见 + L0 显式派发"。

75. **调研配 crawler（v3.9.10 新增 · v3.13.0 两版合并纳入）**：Phase -2 调研 Agent 默认配 `web-scraper`（理解型）/ `hard-site-crawler-playbook`+`agent-browser-core`（硬站点/交互）/ 业务专用 `furniture-competitor-crawler` 不属架构师路径。调研 Agent 任务说明的"上网工具配置"字段按此填写（rule 74 对抗式回路中的 A/B 架构师调研同样适用）。

## Red Flags 可检查信号（v3.7 新增观测器）

以下信号可被外部审计，出现即异常；调度器与审查员据此判分（收纳原第 13 条 symptom）：

- **调度器越权**：发现自己正在写代码 / 拆模块 / 审查 / 改数据 / 写验证脚本 → 立刻停下并路由给对应子 Agent（原第 13 条降级为 symptom）。（机械契约对齐豁免除外：第 49 条规定的 <10 行机械改动 / 模板适配 / 文档落盘类环境修复不视为越权）
- **验证声明超实测**：交付报告中"已验证 ✅"条数 > `cost_log.md` 中实测记录数 → 静默粉饰，FAIL。
- **派发数 ≠ 清单数**：本轮实际子 Agent 调用数 ≠ 派发前列出的"应派模块清单"条数 → 假并行，FAIL。
- **无归因的验证失败回退**：验证失败后未以最小样例复现、未写明"脚本错／代码错"就回退代码 → 违规。
- **架构师缺位**：`cost_log.md` 无架构师派发记录却出现程序员派发 → 违反第 19 条检查，FAIL。
- **未读索引即声称无先验经验（v3.9.3 新增）**：Phase 0.5 初始化未 read_knowledge 检索 `knowledge/_index.md` 就标"已初始化 ✅" → 违反 rule 59/11.5，FAIL（防 NASA 覆辙：不查索引不得声称无先验经验）。
- **蒸馏资产未过人工 Gate / 未追索引（v3.9.3 新增）**：Phase 7 资产落盘未经人工 Gate 确认，或落盘后未更新 `_index.md`（rule 54 Gate）→ 该资产视为未验证，FAIL。
- **验收未过却调度器直改（v3.9.6 新增）**：项目处于 DELIVERED/REOPENED（用户未确认完成）时，调度器对反馈修改请求直接动手（写代码/改文件/调参数）而非路由子 Agent → 违反 rule 63，FAIL（"验收没过 = 项目没完成 = 框架模式持续生效"）。
- **提示词自觉约束未引擎化（v3.9.7 新增）**：发现某项规则完全依赖"调度器记得执行"（如清单核验、状态流转、重复性校验），且可脚本化却未引擎化 → 标注"待加固"（rule 64：能硬约束的绝不靠自觉）——不判 FAIL 但必须列入未验证项清单。
- **未讨论方向就进入下一步（v3.10.0 新增）**：调研/拆解阶段方向 Gate（rule 66 D1-D6）未与用户讨论、decisions_pending.md 无对应确认记录，就派发或进入下一步 → 违反 rule 66，FAIL（"方向先对齐"是硬 Gate，不是建议）。
- **关键模块未过用户终审即合并（v3.11.0 新增）**：关键/高风险模块交付后无"用户终审"留痕（终审卡结论未进 decisions_pending.md / progress.json）就合并进入下一阶段 → 违反 rule 67，FAIL（"最终裁决权留给人"是硬 Gate）。
- **审查无 Rubric 逐项记录（v3.11.0 新增）**：审查员输出无 rule 68 七维度打分（或只有 PASS/FAIL 无维度依据）→ 视为审查不合规，FAIL（防泛泛通过 + 防 Coder-Reviewer 无标准循环）。
- **设计未 CLEAN 即派发（v3.11.0 新增）**：progress.json 的 `design_round` 无 CLEAN 标记就进入 Phase 0 派发 → 违反 rule 71，FAIL（计划评审回合未收敛不得开工）。
- **Wave 复盘失效（v3.11.0 新增）**：连续 2 个 Wave 同一失败模式（rule 70 红线）但 cost_log 无复盘记录/未调整派发参数 → 未验证项清单标注"复盘失效"。
- **无框架痕迹却声称按六层开发（v3.9.12 新增 · galagame 教训）**：项目无 lessons.md/cost_log.md/progress.json/PHASE 标记等任何框架痕迹，交付却声称"按六层框架开发"→ 视为未验证，FAIL（软约束失效，配套 `scripts/six_layer_check.py` 一键核查 + G-006 指南）。
- **非平凡设计外拉双架构师回路（v3.9.10 新增）**：对 trivial 设计/小改动仍拉 rule 74 双架构师 + 挑刺回路（未走 rule 46 编排出口 / rule 20 轻量档）→ 违反成本护栏与 Anthropic 反过度分工原则，标注"待加固"（不判 FAIL 但列未验证项）。

## Resources

### references/task_templates.md
任务说明模板（L0 三件套 + L1 注入 + L3 schema）+ 架构师/程序员/审查员标准模板（各角色已含 `model` 档位字段）。

### references/model_routing.md
模型档位路由（v3.7 新增）：角色 → 派发档位（lite/default/reasoning）映射表 + Agent() 派发示例 + 原生档位的限制说明（仅档位、不指定具体模型名、跨家族校验需自接管 API）。调度器派发前查此表填 `model` 参数。

### references/orchestration_patterns.md
六种多 Agent 编排模式 + 决策矩阵 + 生产注意事项（超时/部分结果/成本），派活前选型用。

### references/spec_and_inputs.md
项目材料包（三类）提交模板 + 规格驱动开发（SDD 四件套 / EARS 验收标准 / 人在回路门禁）合并为一。

### references/reliability.md
可靠性工程：Reflexion 反思循环、失败分类处理、熔断与优雅降级、上下文管理（scratchpad/教训文件/上下文裁剪公式）。

### references/research_workflow.md
调研学习阶段（Phase -2）操作手册：调研方向清单模板、调研 Agent 任务模板（三件套+输出 schema）、调研报告格式、采纳决策标准、成果回流方式、**调研轮询深度钻探法（v3.9 新增）**。

### references/error_routing.md
错误路由器设计（v3.9 新增）：route_error() 纯规则函数（代码级/架构级/合约级关键词 → 目标角色），引擎层实现时写入，不调 LLM。

### references/llm_limits_defense.md
LLM 已知局限与系统防线自查表（v3.9 新增）：12 条局限（7 已撞 + 5 未撞）× 防线 × SKILL.md 落点 × 覆盖状态 + module_type 显式字段设计（编排 B 实现）。

### references/bug_fix_mode.md
Bug-Fix 轻量模式（v3.9.2 新增）：报错修复专用流程——先日志/复现找根因（强制）→ 按根因范围选路（直做/低成本编排/问人）→ 改完必验证 + 交付说明根因。含可粘贴到任意开发窗口的决策卡。

### references/knowledge_loop.md
学习回路原语详设（v3.9.3 新增）：Phase 7 经验蒸馏六步 + 资产四桶 + frontmatter 模板 + 人工 Gate 话术 + read_knowledge 原语（签名/触发点/消费方式/护栏）+ 闭环与 L2 证据（progress.json 召回记录）。rule 58/59/60 的展开文档。

### references/hybrid_review.md
混合审查操作手册（v3.11.0 新增）：AI 初审 + 用户终审全流程、终审卡模板、Rubric 七维度可勾选清单、代码评审/验收分离职责表、Wave 复盘卡模板。rule 67/68/70 的展开文档。

### references/adversarial_design.md
对抗式双架构师设计回路 playbook（v3.9.10 新增 · rule 74）：双架构师 + 挑刺 Agent 的角色定义、Round 1/2 回合协议、proposal_A/B / review / critique / consensus 四套模板、共识门槛与 ≤3 轮熔断升级人流程，非平凡设计阶段默认启用。

### knowledge/（经验资产库）
框架级共享经验库（v3.9.3 新增，跨项目）：四桶 `rules/`（红线，注入 constitution）· `decisions/`（ADR，注入 plan）· `framework-skills/`（可调用 Skill）· `guides/`（可选说明）+ `_index.md`（canonical 索引，read_knowledge 唯一入口）。入口检索原语见 `scripts/read_knowledge.py`。当前指南：G-001 环境坑 · G-002 效果类验收 · G-003 子Agent通道 · G-004 monorepo构建 · G-005 web自动化 · G-006 框架合规检查（galagame 教训，配套 six_layer_check.py）。

### scripts/six_layer_check.py
框架合规检查脚本（v3.9.12 新增 · galagame 教训）：`python scripts/six_layer_check.py <项目目录> [--mode init|deliver|auto] [--strict] [--json]` — 一键核查框架痕迹（git 基线/Phase 0.5 知识资产/SDD 四件套/lessons/cost_log/progress/PHASE 标记/收尾文档），缺失项列清单并提示补哪个 Phase。开工前 `--mode init`、收尾 `--mode deliver`；软约束失效的硬检查解药（rule 64 引擎化落地，详见 G-006）。

### scripts/validate_output.py
L3 输出校验脚本：`python scripts/validate_output.py <output.json> <schema.json>`，exit 0=通过 / 1=失败。

### scripts/checkpoint.py · scripts/cost_log.py · scripts/lifecycle.py
引擎三件套（v3.9.13 新增 · rule 38/39/63 引擎化）：`checkpoint.py` 断点三态（已交付/已验证/未验证）+ 派发前预期产物清单 + 磁盘核验（stat）；`cost_log.py` 成本账本定长行 `[时间]|[对象]|[档位]|[成本]|[结果]|[异常类型]` + 汇总/合规检查；`lifecycle.py` 项目五态状态机（INIT→EXECUTING→DELIVERED→REOPENED/ACCEPTED），非法转移拒绝 + Gate 证据门（DELIVERED 需未验证项清单、ACCEPTED 需用户确认记录）。三件套共用 `progress.json`（lifecycle 字段）。**新项目统一用新格式；旧项目（编排系统 MVP 的 cost_report.json 等）归档不迁移（2026-09-01 决策 A）。引擎先随 skill 版本演进，验证稳定后抽独立仓库（家俊 2026-09-01 决策）。**

### 引擎化落地现状（v3.13.0 更新 · 实测清单）
SKILL.md 描述的引擎能力 vs scripts/ 实际落地（2026-09-01 实测）：✅ `validate_output.py`（L3 校验）· ✅ `read_knowledge.py`（经验召回）· ✅ `six_layer_check.py`（合规检查，v3.9.12）· ✅ `checkpoint.py`（断点三态+预期产物清单，rule 39，v3.9.13）· ✅ `cost_log.py`（成本定长行账本，rule 38/53，v3.9.13）· ✅ `lifecycle.py`（五态状态机+Gate 证据门，rule 63，v3.9.13）· ❌ `cli.py design --finalize`（设计锁定 Gate）· ❌ `route_error()`（错误路由器）。**硬约束层已覆盖核心数据/状态类；剩余引擎化项按 rule 64 优先级逐步补齐，判断类（架构评审/审查）永不脚本化。**
