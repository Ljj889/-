# 交接文档 · six-layer-orchestrator v3.14.0（独立仓库化）

## 1. 项目概况

- **目标**：把六层主 Agent 编排框架从"平台寄生 skill"升级为**可移植、可独立部署、多平台同步**的自有资产——解决 WorkBuddy/CodeBuddy/Codex 三处副本长期分叉、各缺各的问题。
- **范围**：合并 WorkBuddy 版 v3.9.13 与 CodeBuddy 版 v3.12.0 为统一的 v3.13.0，抽独立 git 仓库，同步回三处平台 skills 目录。
- **起止**：2026-09-01（讨论决策）→ 2026-09-01 夜（落地完成）。
- **关键决策**：
  1. 规则编号以 CodeBuddy 1-72 为基准（更完整），WorkBuddy 独有规则追加为 rule 73/74/75 → 全局 1-75、有效 60 条。
  2. G-003 编号冲突：CodeBuddy 版 G-003（子Agent通道）保留原名；WorkBuddy 版 G-003（框架合规）重命名为 **G-006_framework_compliance.md**。
  3. 引擎三件套（checkpoint/cost_log/lifecycle）+ 合规检查（six_layer_check）随 skill 版本演进，验证稳定后再考虑独立引擎仓库（家俊 2026-09-01 决策）。

## 2. 完成情况与产出物

| 产出 | 路径 | 状态 |
|---|---|---|
| 独立仓库 | `D:\Ai\一些ai项目\项目2.0\six-layer-orchestrator\` | ✅ git init + commit 3bc933b + tag v3.13.0 |
| 合并版 SKILL.md | `SKILL.md`（v3.13.0，规则 1-75，有效 60 条，Red Flags 13 条） | ✅ |
| 知识资产库 | `knowledge/` 17 条（R-001~007 + ADR-001~003 + G-001~006 + experience-distiller） | ✅ 索引完整 |
| 参考资料 | `references/` 12 个（含 hybrid_review.md + adversarial_design.md 并集） | ✅ |
| 引擎脚本 | `scripts/` 6 个（validate_output / read_knowledge / six_layer_check / checkpoint / cost_log / lifecycle） | ✅ 全部冒烟通过 |
| 三处同步 | WorkBuddy `~/.workbuddy` · CodeBuddy `~/.codebuddy` · Codex `D:/Ai/codex/skills` | ✅ 三处一致（版本/脚本数/资产数/引用数全部相同） |

**关键指标**：规则 60 条有效 · 知识资产 17 条 · Red Flags 13 条 · 脚本 6 个（✅6 ❌2 引擎化现状）· 三处目录一致性 100%。

## 3. 如何使用 / 运行 / 验证

- **调用**：任意平台（WorkBuddy/CodeBuddy/Codex）对话中触发"用六层框架做 X"即加载本 skill。
- **引擎脚本**（标准库零依赖，Python 3.10+）：
  ```bash
  python scripts/six_layer_check.py <项目目录> --mode init|deliver --strict --json   # 框架合规检查
  python scripts/checkpoint.py status|expect|mark|verify                              # 断点三态 + 预期产物清单
  python scripts/cost_log.py add|summary|tail                                        # 成本账本定长行
  python scripts/lifecycle.py get|transit|history                                     # 五态状态机 + Gate 证据门
  ```
- **验证手段**：三处目录各跑一遍 6 个脚本的 --help/冒烟；`grep -c '^| \(rules\|decisions\|guides\|framework-skills\)/' knowledge/_index.md` 应 = 17。
- **依赖与配置**：无外部 API、无密钥；git 用于版本管理（当前无 remote，未配置凭据）。

## 4. 已知问题 / 风险 / 待办

- ✅ **远程推送完成**：仓库 `https://github.com/Ljj889/-`（当前仓库名为 `-`，创建时未改名——**建议在 GitHub 仓库 Settings → General → Repository name 改为 `six-layer-orchestrator`**，改后本地 remote 同步：`git remote set-url origin https://github.com/Ljj889/six-layer-orchestrator.git`）。master 分支 + v3.13.0 tag 已推送。gh CLI 2.98.0 免安装版在 `D:/tools/bin/gh.exe`（未加入 PATH）。
- ⚠️ **引擎化未全覆盖**：`cli.py design --finalize`（设计锁定 Gate）与 `route_error()`（错误路由器）仍未落地（✅6 ❌2），按 rule 64 优先级后续补齐。
- ✅ 三处同步已固化为脚本：`./sync_to_platforms.sh`（仓库根），支持全量/单平台/`--check` 校验（rule 64：能脚本化的不靠自觉）。
- ✅ WorkBuddy 目录 `.bak` 历史备份已归档到 `archive/skill-history/`（10 个文件，随仓库版本管理，WorkBuddy 目录已干净）。
- 旧版分叉资产已全部并入，无孤儿文件（Codex 原 v3.10.0 已被 v3.13.0 覆盖）。

## 5. 维护与下一步建议

- **⭐ 日常使用工作流（v3.13.1 起，rule 76 强制）**：每次用 skill 完成一个阶段/项目后，经验蒸馏**直接落盘到本仓库**（不是平台 skills 目录）——四步：① Land（资产写仓库 `knowledge/` + 追 `_index.md`）→ ② Commit → ③ Push（`git push origin master`，PowerShell 稳）→ ④ Sync（`bash sync_to_platforms.sh` 分发三处 + `--check` 校验）。**平台 skills 目录是运行时副本不是真相源，禁止直接改。**
- **改版本流程**：编辑仓库 SKILL.md/knowledge → 更新版本日志行 → `git commit` + `git tag vX.Y.Z` → **`bash sync_to_platforms.sh`**（一键同步三处）→ `bash sync_to_platforms.sh --check`（校验）→ `git push origin master --tags`（推送远程）。
- **远程推送凭据备忘**（已配好，后续直接 push 即可）：
  - git 凭据助手 = GCM（Windows 凭据管理器存 Ljj889 OAuth 凭据）；系统级 `credential.helper=helper-selector` 弹窗选过 `manager` 后已记住。
  - 代理：git 已配 `http.proxy=http://127.0.0.1:7897`；PowerShell 下 push 正常（Git Bash 下偶发 SIGTERM 显示问题，实际已推送）。
  - 可选：把 `D:/tools/bin` 加入 PATH 以便随处用 `gh`。
- **下一步可选**：
  1. ✅ 远程推送完成（仓库名待改 `six-layer-orchestrator`）；
  2. ✅ 同步脚本已固化（sync_to_platforms.sh）；
  3. ⏳ 引擎三件套在 1-2 个真实项目跑通后，评估抽独立引擎仓库（rule 64 引擎/智能分离）；
  4. ✅ 旧 `.bak` 已移入 `archive/skill-history/`。
- **接手者先看**：`SKILL.md` 版本行（v3.13.1 合并说明 + rule 76）→ `knowledge/_index.md`（17 条资产入口）→ `引擎化落地现状`段（✅6 ❌2）。

---

## 6. v3.14.0 增强（2026-09-05 · 实战复盘五反馈引擎化）

家俊 Electron 便签项目实战复盘（3 硬伤 + 2 次要）全部落实，rule 77-81（有效规则 61→66）：

1. **rule 77 子代理能力探测与直做模式**：Phase 0 固定 30s 探针（步骤 11.6 + task_templates 探针卡）→ 失败显式 `mode:"direct"` 留痕降级，放宽 rule 12/19 派发形式、验收纪律全保留；Red Flags 越权/缺位条目加 direct 豁免。
2. **rule 78 引擎三件套进 Wave Gate**：checkpoint/cost_log/lifecycle 精确 CLI 固化为每 Wave Gate 动作，新增 Red Flag「Wave 收尾账本/断点无记录」。
3. **rule 79 渲染端静默失效检查 Gate**：`six_layer_check.py render-silent-fail` 子命令（tsconfig include/vite input/tailwind content/preload d.ts/IPC 通道一致性五项，SKIP 防误报，--strict FAIL 即 exit 1）+ knowledge **R-010**；fixture 三 CASE 实测通过。
4. **rule 80 取证优先与现象排查卡**：新建 `references/symptom_triage.md` 三张卡（窗口不可见/样式全丢/IPC 无响应），rule 41 加互链。
5. **rule 81 长排错会话检查点**：假设证伪即落盘 + 3 轮强制小结；bug_fix_mode.md 增补同章节。

**本轮修复的 rule 76 回流断链**：cb 副本曾领先真相源 5 份资产（R-008/R-009/ts-smoke-harness/G-001 v1.2/G-003 v1.1，2026-09-02 蒸馏只落了平台副本）——`sync --check` 资产数差异暴露，已回流（commit `be8fcbe`）。教训：**蒸馏一律 Land 真相源；每次分发后必须跑 `--check`**。

**版本锚点更新**：知识资产 17→21 条（含 R-010）；引擎化现状 six_layer_check 增 render-silent-fail 子命令；commit `bb8724e`（v3.14.0）+ 三平台 sync/--check 全绿；**push 待办**：代理离线未推，家俊代理在线后 `git push origin master`（可顺带打 tag `v3.14.0`）。
