# 交接文档 · six-layer-orchestrator v3.13.0（独立仓库化）

## 1. 项目概况

- **目标**：把六层主 Agent 编排框架从"平台寄生 skill"升级为**可移植、可独立部署、多平台同步**的自有资产——解决 WorkBuddy/CodeBuddy/Codex 三处副本长期分叉、各缺各的问题。
- **范围**：合并 WorkBuddy 版 v3.9.13 与 CodeBuddy 版 v3.12.0 为统一的 v3.13.0，抽独立 git 仓库，同步回三处平台 skills 目录。
- **起止**：2026-09-01（讨论决策）→ 2026-09-01 夜（落地完成）。
- **关键决策**：
  1. 规则编号以 CodeBuddy 1-72 为基准（更完整），WorkBuddy 独有规则追加为 rule 73/74/75 → 全局 1-75、有效 60 条。
  2. G-003 编号冲突：CodeBuddy 版 G-003（子Agent通道）保留原名；WorkBuddy 版 G-003（框架合规）重命名为 **G-006_framework_compliance.md**。
  3. 引擎三件套（checkpoint/cost_log/lifecycle）+ 合规检查（six_layer_check）随 skill 版本演进，验证稳定后再考虑独立引擎仓库（家俊 2026-09-01 决策）。

## 2. 完成情况与产出物

| 产出 | 路径 | 状态 |
|---|---|---|
| 独立仓库 | `D:\Ai\一些ai项目\项目2.0\six-layer-orchestrator\` | ✅ git init + commit 3bc933b + tag v3.13.0 |
| 合并版 SKILL.md | `SKILL.md`（v3.13.0，规则 1-75，有效 60 条，Red Flags 13 条） | ✅ |
| 知识资产库 | `knowledge/` 17 条（R-001~007 + ADR-001~003 + G-001~006 + experience-distiller） | ✅ 索引完整 |
| 参考资料 | `references/` 12 个（含 hybrid_review.md + adversarial_design.md 并集） | ✅ |
| 引擎脚本 | `scripts/` 6 个（validate_output / read_knowledge / six_layer_check / checkpoint / cost_log / lifecycle） | ✅ 全部冒烟通过 |
| 三处同步 | WorkBuddy `~/.workbuddy` · CodeBuddy `~/.codebuddy` · Codex `D:/Ai/codex/skills` | ✅ 三处一致（版本/脚本数/资产数/引用数全部相同） |

**关键指标**：规则 60 条有效 · 知识资产 17 条 · Red Flags 13 条 · 脚本 6 个（✅6 ❌2 引擎化现状）· 三处目录一致性 100%。

## 3. 如何使用 / 运行 / 验证

- **调用**：任意平台（WorkBuddy/CodeBuddy/Codex）对话中触发"用六层框架做 X"即加载本 skill。
- **引擎脚本**（标准库零依赖，Python 3.10+）：
  ```bash
  python scripts/six_layer_check.py <项目目录> --mode init|deliver --strict --json   # 框架合规检查
  python scripts/checkpoint.py status|expect|mark|verify                              # 断点三态 + 预期产物清单
  python scripts/cost_log.py add|summary|tail                                        # 成本账本定长行
  python scripts/lifecycle.py get|transit|history                                     # 五态状态机 + Gate 证据门
  ```
- **验证手段**：三处目录各跑一遍 6 个脚本的 --help/冒烟；`grep -c '^| \(rules\|decisions\|guides\|framework-skills\)/' knowledge/_index.md` 应 = 17。
- **依赖与配置**：无外部 API、无密钥；git 用于版本管理（当前无 remote，未配置凭据）。

## 4. 已知问题 / 风险 / 待办

- ⚠️ **远程推送未完成**：gh CLI 2.98.0 已装（免安装版 `D:/tools/bin/gh.exe`，未加入 PATH），**未登录**。待家俊跑 `gh auth login` 后，一条命令建仓推送（见下方"远程推送指引"）。
- ⚠️ **引擎化未全覆盖**：`cli.py design --finalize`（设计锁定 Gate）与 `route_error()`（错误路由器）仍未落地（✅6 ❌2），按 rule 64 优先级后续补齐。
- ✅ 三处同步已固化为脚本：`./sync_to_platforms.sh`（仓库根），支持全量/单平台/`--check` 校验（rule 64：能脚本化的不靠自觉）。
- ✅ WorkBuddy 目录 `.bak` 历史备份已归档到 `archive/skill-history/`（10 个文件，随仓库版本管理，WorkBuddy 目录已干净）。
- 旧版分叉资产已全部并入，无孤儿文件（Codex 原 v3.10.0 已被 v3.13.0 覆盖）。

## 5. 维护与下一步建议

- **改版本流程**：编辑仓库 SKILL.md/knowledge → 更新版本日志行 → `git commit` + `git tag vX.Y.Z` → **`bash sync_to_platforms.sh`**（一键同步三处）→ `bash sync_to_platforms.sh --check`（校验）。
- **远程推送指引**（gh 免安装版已备好）：
  1. 家俊执行：`D:/tools/bin/gh.exe auth login`（选 GitHub.com → HTTPS → 浏览器授权，本人操作）；
  2. 授权后告诉我，我执行：`gh repo create six-layer-orchestrator --private --source . --push` 一键建私有仓并推送（含 tag v3.13.0）；
  3. 可选：把 `D:/tools/bin` 加入 PATH 以便随处用 `gh`。
- **下一步可选**：
  1. ⏳ 远程推送（gh 已装，等登录）；
  2. ✅ 同步脚本已固化（sync_to_platforms.sh）；
  3. ⏳ 引擎三件套在 1-2 个真实项目跑通后，评估抽独立引擎仓库（rule 64 引擎/智能分离）；
  4. ✅ 旧 `.bak` 已移入 `archive/skill-history/`。
- **接手者先看**：`SKILL.md` 版本行（v3.13.0 合并说明）→ `knowledge/_index.md`（17 条资产入口）→ `引擎化落地现状`段（✅6 ❌2）。
